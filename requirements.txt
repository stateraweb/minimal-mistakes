# Core dependencies
python-dotenv
PyYAML
google-generativeai

# Optional AI integrations
# openai
# huggingface_hub

# Rust is required, but install it manually (not via pip):
# On Termux: pkg install rust